- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#     ~/.vim: vim
#     ~/.vimrc: vimrc
    ~/.zshrc: 
    ~/.gitconfig: 

- create:
    - ~/projects/

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
    - echo "hey guys"
    - command: ./test.zsh
      stdout: true
      stderr: true

